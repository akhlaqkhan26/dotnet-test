kind: pipeline
type: exec
name: default

platform:
  os: linux
  arch: amd64

steps:
- name: build-docker
  commands:
  - docker build -t registry/dotnet-test:1.0.5 .
  - docker push registry/dotnet-test:1.0.5
