kind: pipeline
type: docker
name: default

platform:
  os: linux
  arch: amd64

steps:
- name: build-docker
  image: docker:20
  commands:
  - docker build -t localhost:5000/dotnet-test:1.0.5 .
  - docker push localhost:5000/dotnet-test:1.0.5
